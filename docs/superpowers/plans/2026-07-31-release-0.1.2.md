# Release 0.1.2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Publish version `0.1.2` on GitHub and Modrinth from one verified commit.

**Architecture:** Update the Gradle version and changelog, verify the existing local Modrinth publisher, then create the GitHub tag/release and perform the live Modrinth upload. Credentials remain environment-only.

**Tech Stack:** Gradle, Fabric Loom, mod-publish-plugin, GitHub CLI, Modrinth API.

## Global Constraints

- Use version `0.1.2` for `gradle.properties`, GitHub, and Modrinth.
- Do not store or print the Modrinth token.
- Stop before publication if build, headless game tests, or Modrinth dry run fails.

---

### Task 1: Version Release Metadata

**Files:**
- Modify: `gradle.properties:10`
- Modify: `CHANGELOG.md:5`

- [ ] Change `mod_version=0.1.1` to `mod_version=0.1.2`.
- [ ] Add a dated `## 0.1.2` section before `0.1.1` stating that this release adds local Modrinth publishing and changelog validation.
- [ ] Confirm the changelog contains an exact `## 0.1.2` heading.

### Task 2: Verify Artifacts and Metadata

**Files:**
- Read: `build.gradle`
- Read: `fabric.mod.json`

- [ ] Run `./gradlew build`.
- [ ] Run `./gradlew runGameTest`.
- [ ] Run `./gradlew publishMods -PdryRun`.
- [ ] Confirm the dry run resolves `findmyitems-0.1.2.jar`, Minecraft `26.2`, project `mPTBl3Fj`, and the `0.1.2` changelog section.

### Task 3: Publish GitHub Release

**Files:**
- Use the verified jar: `build/libs/findmyitems-0.1.2.jar`

- [ ] Commit all intended release changes with a release commit.
- [ ] Push `main`.
- [ ] Create and push tag `v0.1.2`.
- [ ] Create and publish the GitHub release with `gh release create v0.1.2 build/libs/findmyitems-0.1.2.jar --title v0.1.2 --generate-notes`.
- [ ] Verify GitHub reports the release as published.

### Task 4: Publish Modrinth Release

- [ ] Run `MODRINTH_TOKEN=... ./gradlew publishMods` without displaying the token.
- [ ] Verify Modrinth reports version `0.1.2` for project `mPTBl3Fj`.
